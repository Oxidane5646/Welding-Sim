using UnityEngine;

namespace Reference_Scripts
{
    public class RayReference : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
